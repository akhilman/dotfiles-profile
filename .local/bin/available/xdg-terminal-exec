#!/bin/sh
case "$XDG_SESSION_DESKTOP" in
	sway )
		if which alacritty > /dev/null; then
			alacritty -e $@
		else
			foot -e $@
		fi
		;;
	gnome )
		if which kgx > /dev/null; then
			kgx -e $@
		else
			gnome-terminal -e $@
		fi
		;;
	xfce )
		xfce4-terminal -e $@
		;;
	* )
		xterm -e $@
		;;
esac
